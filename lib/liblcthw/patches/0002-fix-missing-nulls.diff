--- src/list.c.ORIG	2020-11-25 21:23:40.623260931 +0100
+++ src/list.c	2020-11-25 21:23:45.855301827 +0100
@@ -41,9 +41,11 @@
     if (list->last == NULL) {
         list->first = node;
         list->last = node;
+        node->prev = NULL;
     } else {
         list->last->next = node;
         node->prev = list->last;
+        node->next = NULL;
         list->last = node;
     }
 
